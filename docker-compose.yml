services:
  jupyter:
    build: ./jupyter
    ports:
      - 8001:8001
    volumes:
      - jupyter_data:/src

volumes:
  jupyter_data:
    driver: local
    driver_opts:
      type: none
      device: ./jupyter
      o: bind
